# msgModel - Python Dependencies
# 
# Requires: Python 3.8 or higher
# Install with: pip install -r requirements.txt

# HTTP library for making API requests to LLM providers
requests>=2.31.0

# Anthropic SDK for Claude API (if using Claude)
# Note: The script uses the requests library directly, but you may want
# the official SDK for additional features
# anthropic>=0.18.0
